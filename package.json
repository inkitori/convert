{
  "name": "convert",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "sync:ffmpeg": "node scripts/sync-ffmpeg-core.mjs",
    "dev": "npm run sync:ffmpeg && vite",
    "build": "npm run sync:ffmpeg && tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@ffmpeg/core": "0.12.10",
    "@ffmpeg/ffmpeg": "0.12.15",
    "@ffmpeg/util": "0.12.2",
    "jszip": "3.10.1"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vite": "7.3.5",
    "vitest": "4.1.8"
  }
}
